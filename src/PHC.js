import { useEffect } from "react";

const PHC = () => {
  useEffect(() => {
    document.title = "Northwest Quality Cleaning | PHC";
  })

  return (
    <div id="phc">
      phc
    </div>
  )
}

export default PHC;